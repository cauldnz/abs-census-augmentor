# data/

Runtime-fetched ABS data — ASGS boundaries and Census DataPacks.

The tool populates this directory on first run. Contents are gitignored;
only this README and `.gitignore` are checked in.

See `spec.md` §4 (data sources) and §9 (caching strategy).
