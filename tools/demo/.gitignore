# Generated each time the demos are rendered (host-side pre-warm + the
# Docker tape's own run both write here).
output.csv
preset-output.csv

# render.sh / render.ps1 tee vhs output here for post-render
# diagnostics. Overwritten on every render invocation.
.last-render.log
