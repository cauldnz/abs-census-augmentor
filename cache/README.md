# cache/

Runtime-generated cache — geocoded address lookups (sharded JSON).

The tool populates this directory as it runs. Contents are gitignored;
only this README and `.gitignore` are checked in.

See `spec.md` §7.2 (geocoding cache) and §9 (caching strategy).
